import request from "supertest";
import { Express } from "express-serve-static-core";
import app from "../src/index";

let server: Express;

describe("first", () => {
  beforeAll(() => {
    //
  });

  it("test1", (done) => {
    //
  });
});
